import { OnlyDateDisplayPipe } from './only-date-display.pipe';

describe('OnlyDateDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyDateDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
